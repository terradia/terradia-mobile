mutation loginMutation($email: String!, $password: String!, $exponentPushToken: String) {
    login(email: $email, password: $password, exponentPushToken: $exponentPushToken) {
        token
        userId
    }
}