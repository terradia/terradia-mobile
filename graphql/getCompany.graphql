query getCompany($id: ID!) {
    getCompany(companyId: $id) {
        id
        name
        averageMark
        numberOfMarks
        description
        productsCategories {
            name
            products {
                name
                id
                description
                price
            }
        }
    }
}