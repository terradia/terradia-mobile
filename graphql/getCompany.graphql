query getCompany($id: ID!) {
    getCompany(id: $id) {
        id
        name
        averageMark
        numberOfMarks
        description
        productsCategories {
            name
            products {
                name
                id
                description
            }
        }
    }
}